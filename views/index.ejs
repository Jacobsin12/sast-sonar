<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('partials/header') %> </head>
<body>
    <div class="container d-flex align-items-center justify-content-center min-vh-100">
        <div class="text-center">
            <h1 class="display-2 fw-bold animate-text neon-text">PAPULANDIA </h1>
            <p class="lead animate-text text-muted">La Observabilidad del Futuro, Hoy.</p>
            <div class="mt-5 animate-card">
                <a href="/login" class="btn btn-neon btn-lg px-5 neon-pulse">Comenzar</a>
            </div>
            <div class="mt-3 animate-text text-muted small">Servidor: <%= ip_servidor %></div>
        </div>
    </div>

    <%- include('partials/scripts') %> </body>
</html>